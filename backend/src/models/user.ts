import db from "../../db";
import bcrypt from "bcrypt";

class User {
	static register = (data) => {};
	static authenticate = (username, password) => {};
	static findOne = () => {};
	static findAll = () => {};
	static create = () => {};
	static update = () => {};
	static delete = () => {};
}

export default User;